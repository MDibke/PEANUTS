describe('Demo functions', () => {
    beforeAll(() => {
        // Setup code, si nécessaire
    });

    afterAll(() => {
        // Teardown code, si nécessaire
    });

    test('Demo', async () => {
        expect('foo').toBe('foo');
    });
});
